# Explanation and inference claims addendum v1

This addendum adds an implementation-bound claim only: the system provides a faithful local decomposition of the frozen PCA reconstruction error into normalized per-sensor contributions.

It does not change any confirmatory metric, final-claims-audit classification, detector choice, threshold, calibration, event definition, or test-data boundary. The output is not SHAP, causal evidence, root-cause identification, physical fault localization, or global sensor importance.
